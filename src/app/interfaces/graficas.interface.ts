export interface avancePorTiempo{
    periodo:string,
    total_boletas:number
}
export interface avanceCapturistas{
    capturador:string,
    total_boletas:number
}
export interface avanceEstado{
    estado:string,
    total_boletas:number
}